# Product Overview

ScenarioCore is a proof-of-concept project exploring AI video generation via the RunwayML API. The primary use case is generating short videos from text prompts using Runway's text-to-video models (e.g., gen4.5, gen3a_turbo).

The project is in early prototyping phase with console applications used to test API integration and video generation workflows.
